{
  "name": "common-ekosystem",
  "packageManager": "pnpm@10.1.0",
  "standard": {
    "globals": [
      "describe",
      "it",
      "expect",
      "beforeEach",
      "afterEach",
      "Image"
    ],
    "ignore": [
      "dist/**"
    ]
  },
  "lint-staged": {
    "*.{js,cjs,mjs}": [
      "pnpm lint"
    ]
  },
  "scripts": {
    "prepare": "husky install",
    "lint": "standard 'packages/**/*.js' --fix",
    "lint:check": "pnpm run lint -- 'packages/check/**/*.js'",
    "lint:geokit": "pnpm run lint -- 'packages/geokit/**/*.js'",
    "lint:graphiks": "pnpm run lint -- 'packages/graphiks/**/*.js'",
    "test": "vitest run --coverage",
    "test:check": "pnpm run test --project check",
    "test:geokit": "pnpm run test --project geokit",
    "test:graphiks": "pnpm run test --project graphiks",
    "build": "pnpm -r --if-present build"
  },
  "keywords": [
    "check",
    "graphiks",
    "geokit"
  ],
  "author": {
    "name": "KALISIO <contact@kalisio.com>",
    "url": "https://github.com/kalisio"
  },
  "contributors": [],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/kalisio/basik.git"
  },
  "bugs": {
    "url": "https://github.com/kalisio/basik/issues"
  },
  "homepage": "git://github.com/kalisio/basik#readme",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.17",
    "cross-env": "^10.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "standard": "^17.1.2",
    "vite": "^7.1.9",
    "vitest": "^4.0.17"
  }
}
